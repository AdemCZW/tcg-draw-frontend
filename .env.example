# 複製成 .env.local 後填入自己的值（.env.local 不會進版控）

# PSA Public API token —— 只在本機開發時直連 PSA 用。
# 申請：註冊 psacard.com 帳號後於 https://www.psacard.com/publicapi 取得。
#
# 警告：正式環境「不要」設這個變數。
# 前端 bundle 是公開的，token 會被任何人撈走。
# 正式環境請由後端持有 token，代理 /api/certs/:certNo/images 並把圖快取到 R2。
VITE_PSA_DEV_TOKEN=
